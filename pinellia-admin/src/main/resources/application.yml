server:
  port: 6062

spring:
  application:
    name: admin-server

  cloud:
    # nacos
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
# 端点配置
management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      show-details: always